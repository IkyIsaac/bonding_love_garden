// Placeholder — regenerate from the live schema after each migration:
//   npm --prefix backend run gen:types
// (runs `supabase gen types typescript --local` and writes here)

export type Database = Record<string, never>;
